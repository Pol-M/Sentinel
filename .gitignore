.pio
.vscode/
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
*.o
*.obj
build/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# LaTeX auxiliary/build files (keep the compiled .pdf tracked)
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.bcf
*.run.xml

# Thesis figure assets (large images, keep out of version control;
# the .tex sources and compiled PDFs are still tracked).
docs/**/figures/

# Reference/third-party CAD files (large SolidWorks binaries + archives,
# kept local-only rather than versioned).
CAD/
