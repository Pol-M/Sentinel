// Sentinel — Smart Belay firmware entry point.
//
// This is a placeholder skeleton. Sensor acquisition (rotary encoder, load
// cell, IMU), motor control, and the emergency-brake logic are not yet
// implemented — see docs/DEV_LOG.md for project status and next steps.

#include <Arduino.h>

void setup() {
  Serial.begin(115200);
}

void loop() {
}
